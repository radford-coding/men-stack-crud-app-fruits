<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a Fruit</title>
</head>
<body>
    <h1>Edit <%= fruit.name %></h1>
    <form action="/fruits/<%=fruit._id%>?_method=PUT" method="POST">
        <fieldset aria-labelledby="edit-fruit-legend">
            <legend id="edit-fruit-legend">New Fruit</legend>
            <div>
                <label for="name">Name:</label>
                <input type="text" name="name" id="name" value="<%= fruit.name %>">
            </div>
            <div>
                <label for="isReadyToEat">Ready to eat?</label>
                <input type="checkbox" name="isReadyToEat" id="isReadyToEat" <%= fruit.isReadyToEat ? 'checked' : '' %>>
            </div>
            <div>
                <button type="submit">Update Fruit</button>
            </div>

        </fieldset>
    </form>
    <a href="/fruits/">Back to Fruits</a>
</body>
</html>